# Plan: Apply evolution `ai-agent-langchain/2026-05-12-getmodel-factory-stale.md`

## Goal
Apply proposed changes from the evolution file to `.claude/agents/ai-agent-langchain.md`.

## Steps

- [x] Read current agent file
- [x] Read evolution file
- [x] Apply changes (already applied — all three proposed changes are present in the agent file)
- [x] Verify evolution file frontmatter is up to date
- [x] Finish run

## Findings

All proposed changes were already applied in a prior run (2026-05-22):

1. `lib/getModel.ts` is **not** in the directory map (only `lib/prompts.ts` is listed).
2. The "## Lazy Model Factory" section already states: _"Each agent defines its own `getModel()` locally — the function is intentionally inline, not shared..."_ with no `// lib/getModel.ts` header.
3. "Common Pitfalls" already includes: _"Creating `lib/getModel.ts` expecting agents to import it — they all define their own inline; a shared factory would require updating every agent file."_
4. The note about shared factory migration path is present.

Evolution file frontmatter already shows `status: applied`, `applied_at: 2026-05-22`. No changes needed.
